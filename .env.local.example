# 이 파일을 .env.local 로 복사한 뒤 값을 채우세요. (.env.local 은 git에 커밋되지 않습니다)
# cp .env.local.example .env.local

# Claude API 키 — 서버측에서만 사용. 절대 NEXT_PUBLIC_ 접두사를 붙이지 마세요(클라이언트 노출).
# 발급: https://platform.claude.com/
ANTHROPIC_API_KEY=

# (Phase 4 배포용) 정기 수집 크론 인증 시크릿. MVP 더미 단계에서는 불필요.
# CRON_SECRET=
